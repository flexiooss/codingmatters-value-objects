exampleValue:
  prop: string
  listProp:
    $list: string
  complex:
    sub: string
  complexList:
    $list:
      sub: string

simpleProps:
  stringProp: string
  integerProp: int
  longProp: long
  floatProp: float
  doubleProp: double
  booleanProp: bool
  dateProp: date
  timeProp: time
  dateTimeProp: date-time
  tzDateTimeProp: tz-date-time
